import { createFileRoute } from "@tanstack/react-router";

/** The chat area with no conversation selected; the shell shows its empty state. */
export const Route = createFileRoute("/_workspace/chats/")({});
