diff --git a/dist/ChatStore.js b/dist/ChatStore.js
index 70a08e40c45778f199da2e8a4a1534dd3f85d5e4..074a4218cf42d69524ff1eefca83892452394cc8 100644
--- a/dist/ChatStore.js
+++ b/dist/ChatStore.js
@@ -447,6 +447,7 @@ export class ChatStore {
     applySessionSnapshot(session) {
         const revisionBefore = this.#revision;
         const sessionBefore = this.#session;
+        const hasGoal = Object.prototype.hasOwnProperty.call(session, "goal");
         this.#session = {
             ...withoutKeys(this.#session, [
                 "draft",
@@ -457,7 +458,7 @@ export class ChatStore {
                 "environment",
                 "effort",
                 "folderId",
-                "goal",
+                ...(hasGoal ? ["goal"] : []),
                 "git",
                 "interruption",
                 "lastEventId",
@@ -472,7 +473,7 @@ export class ChatStore {
                 "workspaceId",
             ]),
             archived: session.archived,
-            backgroundProcesses: session.backgroundProcesses ?? [],
+            backgroundProcesses: session.backgroundProcesses ?? this.#session.backgroundProcesses,
             cwd: session.cwd,
             externalTools: session.externalTools ?? [],
             mcpServers: session.mcpServers ?? [],
@@ -501,8 +502,8 @@ export class ChatStore {
             shellCommands: session.shellCommands ?? [],
             skills: session.skills ?? [],
             status: session.status,
-            subagents: session.subagents ?? [],
-            tasks: session.tasks,
+            subagents: session.subagents ?? this.#session.subagents,
+            tasks: session.tasks ?? this.#session.tasks,
             titleStatus: session.titleStatus ?? "idle",
             workflows: session.workflows ?? [],
             workflowsEnabled: session.workflowsEnabled ?? false,
