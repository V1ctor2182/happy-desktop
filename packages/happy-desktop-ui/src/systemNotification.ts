/** The single display line for daemon-injected transcript context. */
export const SYSTEM_NOTIFICATION_LABEL = "System notification";

/** One 20px line with the same 6px vertical inset as a compact tool call. */
export const SYSTEM_NOTIFICATION_HEIGHT = 32;
